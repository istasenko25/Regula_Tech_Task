
# Фильтр Блума для проверки обработки файлов

## 📌 Описание

В рамках тестового задания реализован **фильтр Блума** — эффективная структура данных, предназначенная для проверки, был ли элемент ранее добавлен.
Цель — уметь обрабатывать большое количество путей к файлам и проверять, какие были уже обработаны, даже если система дала сбой.

Реализация включает:
- генератор уникальных имён файлов длиной 255 символов,
- фильтр Блума с возможностью добавления и проверки,
- измерение производительности (время, память),
- минимальное и читаемое решение на Python с использованием только стандартных библиотек.

---

## ⚙️ Структура проекта

- `main.py` — содержит всю логику: генератор, фильтр, запуск, измерения.
- Весь код находится в одном файле, чтобы обеспечить удобную проверку.

---

## 🚀 Как запустить

1. Убедитесь, что у вас установлен Python 3.6+
2. Откройте терминал и перейдите в папку проекта
3. Запустите файл:

```bash
python main.py
```

4. Результаты появятся в консоли:
   - время генерации имён
   - время добавления в фильтр
   - объём используемой памяти
   - проверка наличия существующего и несуществующего имени

---

## 🔍 Пояснение по реализации

Фильтр Блума реализован на основе **списка (`list`)**, где каждый элемент представляет собой бит (0 или 1).

Была протестирована также реализация на **словаре (`dict`)**, но она показала:
- более высокое потребление памяти (примерно в 2.5–3 раза),
- меньшую скорость доступа,
- и не дала преимуществ в удобстве или гибкости.

В финальной версии используется список, так как:
- он быстрее при индексном доступе,
- занимает меньше памяти,
- проще для понимания и масштабирования.

Для хэширования используется встроенная функция `hash((item, k))`, которая обеспечивает достаточно равномерное распределение при высокой скорости выполнения.

---

## 📊 Результаты измерений

```
Тест на 100000 файлов с FPP = 0.01:

- Время генерации: 0.9415 сек
- Время добавления в фильтр: 0.0662 сек
- Память под имена: 781.30 КБ
- Память под фильтр: 7488.38 КБ
- Проверка существующего: 0.000005 сек → True
- Проверка несуществующего: 0.000001 сек → False
```

Финальная реализация на `list` с использованием встроенного `hash()` показала отличную производительность как по времени, так и по памяти.

---

## 🧠 Вывод

Фильтр Блума позволяет эффективно проверять наличие файла в списке уже обработанных без хранения самих значений.
При правильной настройке параметров (число элементов и вероятность ложного срабатывания), фильтр работает быстро и экономно по памяти.

---

## 📐 Обоснование параметров фильтра Блума

Количество элементов (`n`) и вероятность ложного срабатывания (`p`) заданы как входные параметры.
Размер битового массива и число хэш-функций рассчитываются по стандартным формулам:

```
m = - (n * ln(p)) / (ln(2)^2)       — размер битового массива
k = (m / n) * ln(2)                 — количество хэш-функций
```

Это обеспечивает хороший баланс между скоростью, точностью и памятью.